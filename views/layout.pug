doctype html
mixin link(href, text)
	a(href=href)= text

mixin list(...items)
	ul
		each item in items
			li item

html(charset="utf-8")
	head
		block title
			title LocatIn | #{title}
	body
		block content
			p Not yet implented.
